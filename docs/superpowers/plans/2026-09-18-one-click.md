# One-click automatic estimates

User explicitly authorizes automatic prediction and assumptions and wants no required form filling or acknowledgements. Implement directly; source facts remain distinguishable from model estimates.

- [x] Automatic policy module + tests: use fresh eligible institutional paths first, else company history blended with industry medians and bounded fading. Automatically estimate tax, WACC, terminal economics and missing financial components with traceable rules. Preserve raw source company records. For non-FCFF/unsuitable companies use labelled peer relative PB/PE/PS valuation where evidence permits rather than force FCFF. Never manufacture a quoted price or report date. Sparse evidence gets low confidence; completely insufficient evidence may show unavailable without demanding user form input.
- [x] Daily build: produce estimates.json with financialOverrides, assumptions, method, relative inputs, notes, confidence and compact computed summary. Run after collection and before publishing in GitHub Actions, on pushes as well. Keep dataset timestamps and policy version. Verify finite outputs and actual 688256/600519/bank/no-consensus cases.
- [x] UI: stock selection and one-click search immediately evaluate automatic package; remove review checkbox and partial-debt adoption button. Results first, all advanced controls and source tables collapsed. Short confidence/basis text and automatic-assumption provenance in details. Optional advanced editing remains, not required.
- [x] Validate policy, full-universe coverage, browser one-click/mobile, and hosted deployment; avoid rerunning unchanged remote collectors solely for an interface/policy change.

Shared module contract: site/automatic-model.mjs exports buildPeerIndex(companies), buildAutomaticEstimate(company,forecast,peers,now), evaluateAutomatic(company,estimate,assumptions?). Estimate = {method:'fcff'|'relative'|'unavailable', financialOverrides:{}, assumptions: existing compatible shape, notes:string[], confidence:'中'|'低', forecastYears:number, relative?:{metric,unitValue,multiples:number[3],peerCount,scope}, fieldSources?:{}, policyVersion:string}. evaluateAutomatic returns existing compatible valuation result with scenario names bear/base/bull, probabilities from assumptions, weightedValue, optional reverse/sensitivity; relative scenarios have no forecast/bridge. Do not output unsupported buy/sell commands or calibrated probability claims.

Validation: 37 JavaScript + 20 Python tests pass; all 5,581 records generate finite positive automatic outputs. Browser verified Moutai, Cambricon, Ping An Bank and Nanfang Ventilator without required inputs; advanced controls collapsed; 390px viewport has no horizontal overflow. Independent review corrected scenario provenance and removed blocking first-load estimate download. Deployment verification follows in GitHub Pages run.
